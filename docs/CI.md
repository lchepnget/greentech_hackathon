## CI

This repository uses GitHub Actions to run tests and build checks for backend and frontend.

Note: The workflow file in .github/workflows/ci.yml triggers on pushes to main and devops and on pull requests targeting main.
