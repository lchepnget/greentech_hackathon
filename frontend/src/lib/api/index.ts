import {api} from './client'; import type {User,Listing,Order,Wallet,Transaction,Invoice,Paginated,Role} from '$lib/types';
export const auth={login:(email:string,password:string)=>api<{user:User}>('/auth/login',{method:'POST',body:JSON.stringify({email,password})}),register:(data:{email:string;password:string;name:string;role:Role;lightningAddress:string})=>api<{user:User}>('/auth/register',{method:'POST',body:JSON.stringify(data)}),me:()=>api<User>('/auth/me'),logout:()=>api<void>('/auth/logout',{method:'POST'}),forgotPassword:(email:string)=>api<void>('/auth/forgot-password',{method:'POST',body:JSON.stringify({email})}),resetPassword:(token:string,password:string)=>api<void>('/auth/reset-password',{method:'POST',body:JSON.stringify({token,password})})};
export const listings={all:(query='')=>api<Paginated<Listing>>(`/listings${query}`),one:(id:string)=>api<Listing>(`/listings/${id}`),create:(data:Record<string,unknown>|FormData)=>api<Listing & {bolt11?:string}>('/listings',{method:'POST',body:data instanceof FormData?data:JSON.stringify(data)}),remove:(id:string)=>api<void>(`/listings/${id}`,{method:'DELETE'})};
export const orders={all:()=>api<Order[]>('/orders'),create:(listingId:string,quantity:number)=>api<Order>('/orders',{method:'POST',body:JSON.stringify({listingId,quantity})})};
export const invoices={verify:(bolt11:string)=>api<Invoice & {settled?:boolean}>('/invoices/verify',{method:'POST',body:JSON.stringify({bolt11})})};
export const wallet={get:()=>api<Wallet>('/wallet'),transactions:()=>api<Transaction[]>('/wallet/transactions'),deposit:(amountSats:number)=>api<Invoice>('/wallet/deposit',{method:'POST',body:JSON.stringify({amountSats})}),withdraw:(lnAddress:string,amountSats:number)=>api<Transaction>('/wallet/withdraw',{method:'POST',body:JSON.stringify({lnAddress,amountSats})})};
export const users={update:(data:Partial<User>)=>api<User>('/users/me',{method:'PATCH',body:JSON.stringify(data)})};
